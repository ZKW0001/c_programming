#include <stdio.h>

int main () {   
    // int speed = 0;
    // int distance = 22;
    // int time = 5;

    // speed = distance / time;

    // printf("speed = %d\n", speed);

    float speed = 0;
    float distance = 22;
    float time = 5;

    speed = distance / time;

 
    printf("speed = %f\n", speed);

    return 0;
}

